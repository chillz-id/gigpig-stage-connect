/**
 * Event services barrel export.
 * Re-exports all event-related services from the event/ directory.
 * This allows imports like: import { eventApplicationService } from '@/services/event';
 */
export * from './event/index';
